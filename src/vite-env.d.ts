/// <reference types="vite/client" />

// Injected by Vite's `define`. True in the Chrome Web Store build (no debugger
// permission / deep scan); false in the full dev build.
declare const __STORE_BUILD__: boolean
